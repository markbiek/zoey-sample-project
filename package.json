{
	"name": "zoey-sample-project",
	"version": "1.0.0",
	"scripts": {
		"test": "./vendor/bin/pest",
		"setup": "composer install && npm i && npm run migrate && npm run seed",
		"migrate": "vendor/bin/phinx migrate",
		"seed": "vendor/bin/phinx seed:run"
	},
	"author": "Mark Biek",
	"devDependencies": {
		"@prettier/plugin-php": "^0.17.6",
		"prettier": "^2.5.1"
	}
}
